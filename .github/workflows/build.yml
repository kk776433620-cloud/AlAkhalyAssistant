name: بيني
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-java@v5
        with: { java-version: '17', distribution: 'temurin' }

      - name: Build OPENABLE APK
        run: |
          rm -f build.gradle settings.gradle gradle.properties
          rm -rf app build .gradle
          mkdir -p app/src/main/java/com/alakhaly/assistant
          mkdir -p gradle/wrapper

          cat > gradle.properties << 'PROP'
          android.useAndroidX=true
          android.enableJetifier=true
          PROP

          cat > app/src/main/AndroidManifest.xml << 'XML'
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
            <application 
              android:label="AlAkhaly"
              android:theme="@android:style/Theme.Light.NoTitleBar">
              <activity 
                android:name=".MainActivity" 
                android:exported="true"
                android:theme="@android:style/Theme.Light.NoTitleBar">
                <intent-filter>
                  <action android:name="android.intent.action.MAIN"/>
                  <category android:name="android.intent.category.LAUNCHER"/>
                </intent-filter>
              </activity>
            </application>
          </manifest>
          XML

          cat > app/src/main/java/com/alakhaly/assistant/MainActivity.kt << 'KT'
          package com.alakhaly.assistant
          import android.app.Activity
          import android.os.Bundle
          import android.widget.TextView
          import android.view.Gravity
          class MainActivity : Activity() {
            override fun onCreate(savedInstanceState: Bundle?) {
              super.onCreate(savedInstanceState)
              val tv = TextView(this)
              tv.text = "AlAkhaly Assistant\n\n✅ تم التصليح!\nالتطبيق يفتح الآن بنجاح\n\nالنسخة 21"
              tv.textSize = 22f
              tv.gravity = Gravity.CENTER
              tv.setPadding(50,200,50,50)
              setContentView(tv)
            }
          }
          KT

          cat > app/build.gradle << 'APP'
          plugins { id 'com.android.application'; id 'org.jetbrains.kotlin.android' }
          android {
            namespace 'com.alakhaly.assistant'
            compileSdk 33
            defaultConfig { applicationId "com.alakhaly.assistant"; minSdk 21; targetSdk 33; versionCode 21; versionName "21.0-OPEN" }
            compileOptions { sourceCompatibility JavaVersion.VERSION_17; targetCompatibility JavaVersion.VERSION_17 }
            kotlinOptions { jvmTarget = '17' }
          }
          dependencies {
            implementation 'androidx.core:core-ktx:1.10.1'
          }
          APP

          cat > build.gradle << 'ROOT'
          buildscript {
            repositories { google(); mavenCentral() }
            dependencies {
              classpath 'com.android.tools.build:gradle:7.4.2'
              classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.20'
            }
          }
          allprojects { repositories { google(); mavenCentral() } }
          ROOT

          cat > settings.gradle << 'SET'
          include ':app'
          SET

          echo "distributionUrl=https\://services.gradle.org/distributions/gradle-7.6-bin.zip" > gradle/wrapper/gradle-wrapper.properties

      - uses: gradle/actions/setup-gradle@v4
        with: { gradle-version: 7.6 }
      - run: gradle assembleDebug --no-daemon
      - uses: actions/upload-artifact@v4
        with: { name: AlAkhali-APK-OPEN, path: app/build/outputs/apk/debug/*.apk }
